# Промпт для следующей сессии (сессия 62)

Ты — оркестратор. Пользователь за компом есть, но его время дорого: всё, что достижимо без него
(пустыня, сборки, тесты, ревью), делаешь сам, а зовёшь только для Sky Garden — довести игру до
сцены и отпустить ввод. Цель — **60 FPS в Sky Garden**. Сейчас ~29–30 FPS: CPU потока GuestGpu
32–34 мс, GPU 13,6–15 мс, ~4 850 draw, 6,64–6,98 мкс CPU на draw в базах.

## 0. Что прочитать первым (сам)

`docs/parallel-draw-path.md` §0 (итог сессии 61), §0a (60), §0b (59); `docs/local-session-61.md`
(особенно §2 профиль и §6 зависание входа); начало `docs/cpu-hot-path-map.md` (блоки сессий 61 и
60); блок сессии 61 в `CLAUDE.md`/`HANDOFF.md` папки эмулятора.

## 1. Решено измерениями — не переоткрывать

- **Профиль потока после `progmemo` снят** (`C:/kyty/s61/prof_sky61a.txt`): `PrepareBindings`
  7,1 %, `EndRecord` 4,9 % (одна инструкция `xchg` публикации головы кольца записи),
  `VirtualProtect` 4,7 %, `VerifyWitness` 4,5 %, `TryReadBacking` 3,8 %, `CommitBindings` 3,6 %,
  `AheadTake` 3,1 %, `GetGraphicsPrograms` 3,1 %, спин-лок трекера 2,8 %, `QueueAheadSource` 1,9 %.
- **Загрузки буферов пакетом закрыты потолком** (≈1 %: 40 из 66 подряд, но разрывов прохода по
  ним 16–19 на кадр). **Свидетель M1 по эпохам регионов закрыт потолком** (44 % попаданий с
  уже сдвинутой эпохой при `da_stale` = 0).
- **Проверка свидетеля M1 стоит ≈2,3 мс на кадр** (`dawitness=0` — небезопасный замер, −6,9 /
  −3,4 %); `dawitptr` (указатели воркера + префетч) и `daqpre` (префетч слотов) — по ≈0,4 мс по
  таймерам, в CPU на draw с потоком записи ≈0 (без него −1,2…−2,7 % парой). **Гейты 0.**
- **`recpubn` (публикация головы кольца раз в N записей) и `recrelax` — 0:** цена `xchg`
  переезжает в дренажи 780 прямых записей на кадр (`rec_direct_busy` 31 → 210).
- Сессии 57–60: `progmemo` 1, `rtfast` 1, `dawalk` 0, `armdefer` 0, B9 закрыт — см.
  `docs/next-session-61.md` §1.

## 2. Работы сессии (по отношению выгода/риск)

1. **Зависание первого входа в Sky Garden после сборки** — 2 из 4 заходов сессии 61: оба
   зависания — первый вход после новой сборки, все три удачных входа — повторные с тем же бинарём
   (первый тяжёлый кадр уровня после загрузочного тоннеля, `nvlddmkm 153`, `acopy` в порядке; §6
   отчёта). То же правило нашли в сессии 54 (тогда — после правок кэша трансляции; здесь подпись
   кэша не менялась, механизм не назван). Первый заход сессии — на свежей сборке:
   `launch_play.py <tag> "..." KYTY_PIPELINE_CACHE=0 KYTY_GPU_CHECKPOINTS=1
   KYTY_GPU_HANG_ABORT_S=0` — при повторении breadcrumb назовёт зависшую операцию и шейдер
   (поток записи при чекпоинтах выключен — свип в этом заходе только для M1-правок). Пока
   механизм не найден: **после каждой сборки первый вход в сцену — прогревочный**, замер — со
   второго; `progmemo=0` на входе не нужен.
2. **Прямые записи в пакет** (`rec_direct` ≈780 на кадр: `RecordBufferCopies`, барьеры, transit'ы
   вне пакета — сначала счётчик по сайтам `Handle()`): без них публикацию можно делать редкой
   (`recpubn` уже есть) и взять 4,6 % `EndRecord`; сами дренажи `rec_direct_busy` 31–45 на кадр.
3. **`TryReadBacking` 3,8 %** — копии констант в stream-ring (`cb_copy` 7 500 на кадр, 730 КБ,
   `NativeUpload`/`ObtainBuffer`): счётчик потолка — доля копий, чей диапазон не менялся с прошлой
   копии (`RangeWriteEpoch`), затем memo по эпохе.
4. **M2** — только с числами §6 сессии 59 и после 1–3.

## 3. Методика

Как в сессиях 57–61: фазы по 500 present, A B A B, три повтора, порог ±1,4 %, решение по полосам
`bands.py`, потолок счётчиком до реализации. **Троттлинг:** в `sky61d` на 8-й фазе базы ушли с
6,7 на 7,9 мкс/draw при ядрах 7 → 8,2 — смотреть `cores` и базы, вторую половину длинного свипа
не засчитывать; свипы не длиннее ~25 фаз. Гейты записи — без `KYTY_GPU_TIME`.

## 4. Прогоны и инструменты

Скопировать из `C:/kyty/s61/` в `C:/kyty/s62/` и поменять `root`: `launch_run.py`, `measure_run.py`,
`summary3.py`, `bands.py`, `launch_play.py`, `launch_play_exe.py`, `sky_sweep.py`,
`samples_phase.py`, `counters.py`, `patchlib.py`, `g.py`. Данные сессии 61: `prof_sky61a.txt`,
`summary3_sky61*.json`, `gates_sky61*.json`, `log_sky61*.txt`, `rec_sky61*.mp4`,
`log_sky61a_crash.txt`/`log_sky61b.txt` (зависания). Вход в сцену: `launch_play.py <tag>
"drawahead=1 asyncsubmit=1 imgrecycle=1 dathreads=4" KYTY_PIPELINE_CACHE=0 KYTY_QUEUE_TRACE=1`
(первый вход после сборки — прогревочный).

## 5. Правила

Патч-скрипты через `patchlib.py` (`PATCH_DRY=1`; C-строки с `\n` — только файлом через Write, не
heredoc'ом); счётчики — в конец enum `frameStats.h` + `named[]` в `videoOut.cpp`; гейты —
`common/gates.*`, умолчание только по замеру. Сборка `cmd //c "C:\kyty\build_local.cmd"` — **не
запускать параллельно с замером** (свип `rp` сессии 61 испорчен сборкой: базы 8 → 15 мкс/draw);
тесты `cmd //c "C:\kyty\s52\build_tests.cmd"` + пять бинарей (+ `memory_tracker_tests` с
`KYTY_ARM_DEFER=1`). Перед коммитом `python C:/kyty/normalize_eol.py`. В конце — коммит без
спроса, push нет; обновить `HANDOFF.md`, `CLAUDE.md`, `AGENTS.md`, отчёт и docs. Игру лишний раз не
запускать.
