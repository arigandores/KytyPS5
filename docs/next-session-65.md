Ты — оркестратор. Всё, что достижимо без пользователя (пустыня, сборки, тесты, ревью), делаешь
сам; пользователя зовёшь только для Sky Garden — довести игру до сцены и отпустить ввод (свип
стартует сам: `sky_auto.py`). Цель — **60 FPS в Sky Garden**. Сейчас 30,0 FPS (с `recpin`, базы
`sky63a`): CPU потока GuestGpu 31,2–31,6 мс, GPU 14,4–15,4 мс, ~4 840 draw, 6,37–6,44 мкс CPU на
draw. В сессии 64 Sky Garden не запускался.

## 0. Что прочитать первым (сам)

`docs/parallel-draw-path.md` §0 (итог сессии 64: E4/E6 на пустыне), §0a (63), §5 (трек C), §6
(таблица измерений — E1/E2/E4/E6 теперь сняты); `docs/local-session-64.md` (§1 таблица, §2
методика, состав проб); начало `docs/cpu-hot-path-map.md`; блок сессии 64 в `CLAUDE.md`/`HANDOFF.md`
папки эмулятора.

## 1. Решено измерениями — не переоткрывать

- **Пустыня, сессия 64:** валовая цена читающей части разрешения привязок на потоке GuestGpu
  (`shadowinline=1`, E6) — **+6,1…+7,0 % CPU на draw** (342 мкс на кадр); налог K читателей
  (`shadowresolve=K`, E4) — K=1 **+4,1…+4,6 %**, K=2 +4,2…+6,3 %, K=4 **+7,5…+8,2 %**, почти не
  зависит от того, что читают (`shadowmask` 1/2 дают столько же). Воркер тратит на те же чтения
  2,6× времени критического потока. Состав: образов по записанному view 96 %; буферов по memo/эпохе
  31 %, копия в кольцо 19 %, `SynchronizeBuffer` 47 % — draw, разрешимых целиком, почти нет.
  Протокол пробуждения — статья первого порядка (notify на каждый push стоил 7 % потока; задания
  ставить пачками, как M1).
- Сессия 63: публикация кольца записи закрыта (`recpin` 1). Сессии 57–62: `progmemo` 1, `rtfast` 1,
  `dawalk`/`armdefer`/`dawitptr`/`daqpre`/`recimg`/`recup`/`recpubn` 0, B9, загрузки пакетом,
  свидетель M1 по эпохам, memo копий констант — потолком.

## 2. Работа сессии

**Единственный пункт — свип `sky64a` в Sky Garden** (`C:/kyty/s64/phases_sky64a.json`, 25 фаз по
500 present: inline ×3, K=4 ×3, K=4 только образы ×2, K=4 только буферы ×2, K=1 ×2, K=2 ×1, базы
между). Он решает судьбу M2:
- налог K=1 в сцене ≥ цены `shadowinline` → **M2 закрыт потолком**, C1–C3 не начинать, план
  §5 закрывается; сессию дальше тратить на GPU-сторону (14,4–15,4 мс у бюджета) или на число draw;
- налог меньше → чистый выигрыш = (доля `sh_inline_us` в потоке) × (доля draw, разрешимых
  целиком: `sh_img_fast`/`sh_img` и `sh_buf_fast`+`sh_buf_epoch` (+`sh_buf_stream` при C1) по
  draw) − налог; обсуждать C1 только при результате выше порога 1,4 %.
Ничего не реализовывать до этих чисел. Если пользователя нет — сессия заканчивается отчётом с
теми же числами и этим же промптом.

## 3. Методика и команды

Как в сессиях 57–64: фазы по 500 present, A B A B, три повтора, порог ±1,4 %, `bands.py`; первая
база прогревочная; gate-файл — имена, отсутствующие в файле, сохраняют состояние; свипы ≤ 25 фаз,
смотреть `cores` (воркеры `shadowresolve` поднимают ядра на 1,5–2). Скопировать `C:/kyty/s64/*.py`
в `C:/kyty/s65/`, заменить `root`. Вход в сцену:
```
python C:/kyty/s65/launch_play.py sky65a "drawahead=1 asyncsubmit=1 imgrecycle=1 dathreads=4 shadowinline=0 shadowresolve=0 shadowmask=3" KYTY_PIPELINE_CACHE=0 KYTY_QUEUE_TRACE=1
python C:/kyty/s65/sky_auto.py <pid> sky65a C:/kyty/s64/phases_sky64a.json 500
```
пользователь доводит до Sky Garden и отпускает ввод; после свипа скопировать `_run_stdout.txt`
вручную; сводка `summary3.py sky65a 4700 5100 sh_us sh_inline_us sh_push_us sh_lock_us sh_trk_us
spin_gpu_us sh_drop`. При зависании входа — `KYTY_GPU_CHECKPOINTS=1 KYTY_GPU_HANG_ABORT_S=0`.

## 4. Правила

Патч-скрипты через `patchlib.py` (`PATCH_DRY=1`; C-строки с `\n` — только файлом через Write);
счётчики — в конец enum `frameStats.h` + `named[]` в `videoOut.cpp`; гейты — `common/gates.*`.
Сборка `cmd //c "C:\kyty\build_local.cmd"` — не параллельно с замером; тесты
`cmd //c "C:\kyty\s52\build_tests.cmd"` + пять бинарей (+ `memory_tracker_tests` с
`KYTY_ARM_DEFER=1`). Перед коммитом `python C:/kyty/normalize_eol.py`. В конце — коммит без
спроса, push нет; обновить `HANDOFF.md`, `CLAUDE.md`, `AGENTS.md`, отчёт и docs. Игру лишний раз
не запускать; stream-кольцо с CPU не читать. Гейты `shadowresolve`/`shadowinline` —
измерительные: пробы читают слоты кэшей без защиты от вставки/удаления потоком GuestGpu, в игру
их не включать.
