Ты — оркестратор. Всё, что достижимо без пользователя (пустыня, сборки, тесты, ревью), делаешь
сам; пользователя зовёшь только для Sky Garden — довести игру до сцены и отпустить ввод (свип
стартует сам: `sky_auto.py`). Цель — **60 FPS в Sky Garden**. Сейчас 29,6–30,1 FPS (базы `sky64a`
= `sky63a` с `recpin`): CPU потока GuestGpu 31,0–31,5 мс, GPU 14,1–14,8 мс, ~4 860 draw,
6,29–6,64 мкс CPU на draw.

## 0. Что прочитать первым (сам)

`docs/parallel-draw-path.md` §0 (итог сессии 64: трек C закрыт), §0a (63), §9 (что закрыто);
`docs/local-session-64.md` (§1 таблицы пустыни и Sky Garden, §2 методика); начало
`docs/cpu-hot-path-map.md`; блок сессии 64 в `CLAUDE.md`/`HANDOFF.md` папки эмулятора.

## 1. Решено измерениями — не переоткрывать

- **Трек C (M2, C1–C4) закрыт потолком (сессия 64).** Читающая часть разрешения привязок —
  6–7 % потока GuestGpu (`shadowinline`: пустыня +6,1…+7,0 %, Sky Garden +1,7…+5,9 %, счётчиком
  2,2–2,4 мс из 31); налог одного читающего воркера на поток — столько же (`shadowresolve=1`:
  +4,3 % / +6,3 %), четырёх — +7,9 % / +6 %, одна постановка задания в сцене — 1,5 % потока; воркер
  делает те же чтения в 1,8–2,6× дольше; половине буферных привязок (~9 на draw) нужен
  `SynchronizeBuffer`, draw, разрешимых целиком, почти нет. Не переоткрывать ни с локами (C3), ни с
  приватизацией (C1): налог не в локе (`spin_gpu_us` ≤ 0,6 %), а в передаче и разделяемых линиях.
- Сессии 57–63: `progmemo` 1, `rtfast` 1, `recpin` 1; `dawalk`, `armdefer`, `dawitptr`, `daqpre`,
  `recimg`, `recup`, `recpubn` — 0; B9, загрузки пакетом, свидетель M1 по эпохам, memo копий
  констант, общий механизм прямых записей — потолком. Профиль после `recpin` плоский: крупнейшая
  статья 6,5 %, все с закрытыми потолками.
- **В плане `parallel-draw-path.md` не осталось пункта с измеренным потолком выше порога 1,4 %.**
  60 FPS по CPU (31 → 16,6 мс) не достижимы ни одним из измеренных механизмов.

## 2. Работа сессии — сначала вопрос пользователю

Это решение пользователя, задать его до любой работы (AskUserQuestion), варианты:
1. **GPU-сторона.** GPU занят 14,1–15,4 мс при бюджете 16,6 — стена сразу за CPU, и при любом
   CPU-выигрыше FPS упрётся в неё. Сначала потолок: `KYTY_GPU_TIME=1` в Sky Garden
   (`GpuTime-kind:`/`GpuTime-top:` по проходам и шейдерам, разбор `scripts/s14_gputime.py`; помнить,
   что с `KYTY_GPU_TIME=1` пакетов записи нет — только GPU-числа), затем RenderDoc-захват сцены
   пользователем (F1) и `rd_gputime.py`. Кандидаты известны по катсцене: CS освещения `5323`,
   полноэкранные PS, DOF/TAA.
2. **Число draw/привязок на стороне трансляции PM4.** Сначала счётчик потолка: соседние draw с
   тем же pipeline, теми же дескрипторами и V# (отличаются только user SGPR / индексами) —
   кандидаты на слияние; `e9_adj` 79 % наборов подряд с тем же pipeline (сессия 57) — верхняя
   граница. Без счётчика не начинать; слияние меняет семантику GPU — риск высокий.
3. **Инструментация (E8):** одна фаза совсем без `KYTY_FRAME_TRACE` (сравнение по `AvTrace: flip`)
   — сколько стоит `lite` в сцене (на пустыне 1,8 мс). Не выигрыш, а честность чисел.
4. **Зафиксировать результат** (30 FPS) и закрыть план.

Ничего не реализовывать до ответа. Если пользователя нет — снять пп. 1 и 3 потолками там, где
не нужен пользователь (пустыня), и закончить отчётом с этим же вопросом.

## 3. Методика и команды

Как в сессиях 57–64: фазы по 500 present, A B A B, три повтора, порог ±1,4 %, `bands.py`; первая
база прогревочная; gate-файл — имена, отсутствующие в файле, сохраняют состояние; свипы ≤ 25 фаз,
смотреть `cores`; браковать базы, ушедшие на 3 % и больше (в `sky64a` таких три). Скопировать
`C:/kyty/s64/*.py` в `C:/kyty/s65/`, заменить `root`. Вход в сцену:
```
python C:/kyty/s65/launch_play.py <tag> "drawahead=1 asyncsubmit=1 imgrecycle=1 dathreads=4" KYTY_PIPELINE_CACHE=0 KYTY_QUEUE_TRACE=1
python C:/kyty/s65/sky_auto.py <pid> <tag> <phases.json> 500
```
после свипа скопировать `_run_stdout.txt` вручную. При зависании входа —
`KYTY_GPU_CHECKPOINTS=1 KYTY_GPU_HANG_ABORT_S=0`.

## 4. Правила

Патч-скрипты через `patchlib.py` (`PATCH_DRY=1`; C-строки с `\n` — только файлом через Write);
счётчики — в конец enum `frameStats.h` + `named[]` в `videoOut.cpp`; гейты — `common/gates.*`,
умолчание только по замеру. Сборка `cmd //c "C:\kyty\build_local.cmd"` — не параллельно с
замером; тесты `cmd //c "C:\kyty\s52\build_tests.cmd"` + пять бинарей (+ `memory_tracker_tests`
с `KYTY_ARM_DEFER=1`). Перед коммитом `python C:/kyty/normalize_eol.py`. В конце — коммит без
спроса, push нет; обновить `HANDOFF.md`, `CLAUDE.md`, `AGENTS.md`, отчёт и docs. Игру лишний раз
не запускать; stream-кольцо с CPU не читать. Гейты `shadowresolve`/`shadowinline`/`shadowmask`
— измерительные, в игру не включать.
