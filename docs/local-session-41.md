# Сессия 41 — проверка после WIP debf442

2026-09-10: пользователь закрыл другую игру и разрешил продолжить. База —
`merge-upstream debf442`; бинарь от сессии 40, исходники не менялись.
Посторонние удаления PNG в `3rdparty/nlohmann_json` не затрагивались.

## Восстановление каталога

Проверены размеры и SHA256 файлов кандидата `s40/seed_candidate/PPSA21564`.
Исторический установленный seed сохранён в `s41/seed_previous/PPSA21564`,
локальный каталог — в `s41/cache_before_restore/PPSA21564`. Пути проверены до
перемещения. Установлен новый seed 383 шейдера / 447 рецептов.

Запуск `KYTY_CACHE_MODE=prepare-cold` при отсутствующем локальном каталоге:

- `ShaderSeed: installed (383 shaders)`;
- **447/447, 0 skipped**, 350 graphics + 97 compute;
- `PipelinePrecache: all pipelines created, 22285 ms`;
- дополнительное ожидание `10448 ms`;
- `ShaderPreparation: ready` предшествует `Execute: Main`.

Одновременно включена обычная Vulkan-валидация. Эти времена описывают подготовку
под валидацией, не являются чистым сравнением скорости с прежним запуском без неё.
Режим prepare-cold не перезаписывает обычный драйверный кэш и солит новые модули; это
проверка восстановления и свежей компиляции известного набора, а не всех сцен игры.

## Игровая проверка

Выполнен маршрут сессии 40 с удержаниями по present, встряхиванием R, прыжками,
движением к склону и пятью дополнительными удержаниями W. Все эффекты включены.
`--vulkan-validation true`, фильтров сообщений нет; отдельная synchronization
validation всего маршрута в этом прогоне не включена. Фокусные sync-тесты BDA
и read-only depth уже прошли в сессии 40 и без изменения кода не повторялись.

Результат длительного маршрута и проверка видео приведены в разделе завершённых проверок.
Предупреждения о неиспользуемом fragment output Location 1 учитываются отдельно от ошибок.

Артефакты: `install_and_validate.ps1`, `run.ps1`, `audit_live.py`,
`validation_audit.json`, `rec_restore_validate.mp4` и его `.idx`,
`C:/kyty/log_s14_s41_restore_validate.txt` (после завершения).

## Производительность и общий запуск игр

В этом этапе закрываются отложенные проверки сессии 40; новых изменений рендерера
не внедрено. Под валидацией fps не используется для сравнения производительности.
Остаётся CPU-предел при тысячах draw песчаного эффекта: ранее до ~34 мс при ~2000 draw,
худшее окно 60 present — 27,05 fps. Стабильные 60 при нескольких следах не подтверждены.

Для общего запуска игр сохраняется решение `docs/shader-startup-design.md`:
автоматический сбор/подготовка известных рецептов уже общий для title ID; долговечный
guest replay-каталог для восстановления после обновлений транслятора ещё нужно реализовать.
Ручной разбор каждой сцены не является нормальным пользовательским workflow.

## Завершённые проверки

- Обычная Vulkan-валидация без фильтров: **21066 present / 1658.891 с
  от первого present**, **0 ошибок Vulkan**. Пройдены интро, переход к управлению,
  бег/прыжки, склон и пять дополнительных удержаний W; последнее отпущено на present 19000.
- Есть 10 зарегистрированных предупреждений семейства неиспользуемого fragment output
  Location 1 (дальше действует штатный duplicate_message_limit слоя). Ноль ошибок не означает
  ноль предупреждений. Полный маршрут с отдельной synchronization validation не запускался.
- Видео **rec_restore_validate_review.mp4: 20469 кадров, decode exit 0, decode_errors пуст**.
  Один подтверждённый эпизод A-B-A в анимации начального меню: video frame 543, present 544,
  host 24,591 с. Причина не установлена; не объявлять всю запись визуально чистой. На игровом
  участке детектор других кандидатов не нашёл. `video_inspection.json`, `menu_transition.mp4`.
  Контрольные кадры извлечены только из записи.
- Короткий обычный запуск меню без валидации: **60,00 fps** в host-окне 5–55 с,
  `rec_menu_control_review.mp4` — **3081 кадр, 0 кандидатов, decode clean**.
  Эпизод меню в этом контроле не повторился; это не устанавливает его причину.
  Тёплая подготовка **448/448, 0 skipped**, 164 мс всего / 36 мс ожидания. Один новый
  рецепт добавился автоматически во время длинного маршрута; установленный seed остаётся 383/447.
- Восстановление seed 383/447 и prepare-cold прошли. Подготовка 22,285 с, дополнительное
  ожидание 10,448 с; проверка выполнялась с обычной Vulkan-валидацией. Обычный driver-cache
  после валидации совпал по SHA256 с сохранённой копией хэша; обычный контроль меню проводился позже.
- Установленный seed проверен по manifest SHA256; локальный каталог восстановлен автоматически.
  Исходники и бинарь остались от debf442. Бинарь SHA256 `8FA1B7DEF075F2556C93BC328C3E40560AE4DD1873A680F2DC14B0AB61853448`, встроенная метка прежняя
  `d809a0b-dirty`; пересборка только ради метки не делалась. Shader source hash
  `372eefb2161e3659366700c020877c4dd8b0d77a`.

Отложенные проверки Vulkan и восстановления seed выполнены; визуальная проверка выявила
отдельный однокадровый эпизод меню, требующий разбирательства. Это не проверка всей
игры, не обещание 60 fps и не универсальный меж-GPU каталог. Проблема CPU при множестве
следов остаётся следующей задачей; shader replay-каталог пока только спроектирован.
